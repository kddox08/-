# ChattingApp

A tiny browser-based chat demo built with plain HTML, CSS, and JavaScript.

## Run locally

Because this app uses static files only, you can serve it with any static server.

Example using Python:

```bash
python3 -m http.server 4173
```

Then open `http://localhost:4173`.
